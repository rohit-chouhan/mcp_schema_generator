name: mcp_schema_generator
resolution: workspace
description: A starting point for Dart libraries or applications.
version: 1.0.0-beta.1
repository: https://github.com/rohit-chouhan/mcp_schema_generator

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  mcp_annotations: ^1.0.0-beta.1
  mcp_runtime: ^1.0.0-beta.1
  source_gen: ^4.2.3
  analyzer: ^10.2.0
  build: ^4.0.6
  code_builder: ^4.11.1
  dart_style: ^3.1.5

dev_dependencies:
  build_runner: ^2.15.0
  lints: ^6.0.0
  source_gen_test: ^1.3.6
  test: ^1.25.6
